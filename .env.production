# Committed templates — copy the one you need to `.env` (gitignored).
# Production project doesn't exist yet. When it does, paste the real URL + anon
# key here. Boot FAILS LOUDLY if these look like placeholders (see lib/config.ts).
# Never commit this file with real values — keep it a template.
EXPO_PUBLIC_SUPABASE_URL=https://xyzcompany-supabase-prod.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.production-placeholder
EXPO_PUBLIC_SMS_PROVIDER=disabled
EXPO_PUBLIC_APP_ENV=production
EXPO_PUBLIC_LOG_LEVEL=warn
EXPO_PUBLIC_ERROR_REPORTER=console
# EXPO_PUBLIC_SENTRY_DSN=
# EXPO_PUBLIC_POSTHOG_KEY=
