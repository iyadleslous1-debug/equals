# Ownership is solo for now. Replace the handle below with your GitHub
# username when you connect the repo, and add one line per area as the team grows:
#   /supabase/  @backend-dev
#   /app/ /features/  @frontend-dev
* @YOUR_GITHUB_HANDLE
